.DS_Store
DerivedData/
*.xcuserstate
xcuserdata/
*.xcbkptlist
*.xcuserstate
build/
.build/
.swiftpm/

# Bundled on-device LLM model (~337MB) -- kept on disk for local archive
# builds to embed, but too large for plain git (GitHub hard-blocks files
# over 100MB without Git LFS).
VoxbriefApp/Resources/Models/
